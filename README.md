# Git Rebase vs. Git Merge

This is a demo repo to help show the difference between `git merge` and `git rebase`.

You can find the explanation of `git merge` in the [git-merge](git-merge/README.md) folder and the explanation of `git rebase` in the [git-rebase](git-rebase/README.md) folder.
